# MOSS runtime 的 CPU 变体依赖（无 NVIDIA GPU 机器首装用）。
#
# 与 moss-requirements.in 的唯一差别：torch / torchaudio 使用 PyPI 原生 CPU
# 构建（不带 cu130 本地版本号、不指向 pytorch-cu130 index）。因为
# moss-requirements.in 直接 pin 了 torch / torchaudio 的 cu130 本地版本，
# 同一份文件去掉 extra index 无法解析出 CPU 版本，所以按 local-cpu 先例
# 独立声明、独立冻结：
#
#   uv pip compile moss-cpu-requirements.in -p 3.11 \
#     --generate-hashes --index-strategy unsafe-best-match \
#     -o build/requirements-moss-cpu.txt
#
# --generate-hashes 让 CPU wheel 拥有与版本匹配的真实哈希。
# 该变体仅被 sys_platform != 'darwin' 且无 NVIDIA GPU 的机器消费，因此不
# 携带 darwin marker 行；其余直接依赖必须与 moss-requirements.in 保持一致
# （tests/test_packaging_contract.py 有版本对齐断言，升级时同步修改）。
av>=14.0
librosa>=0.11.0
numba>=0.61.0
packaging>=24.0
safetensors>=0.6.2
soundfile>=0.12
soxr>=0.5
transformers>=5.6.0,<6.0.0
moss-transcribe-diarize @ https://github.com/OpenMOSS/MOSS-Transcribe-Diarize/archive/e607537b1b870475e7898969d40b864de8b691b6.zip
torch==2.13.0
torchaudio==2.11.0
