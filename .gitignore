.env
.venv/
__pycache__/
*.py[cod]
.pytest_cache/

# Derived editor and waveform data. Keep the JSON project and the original media outside the repository when possible.
*.waveform.json
*.json.bak
*.edit.html
!blank-editor.html

# Local media and subtitle outputs
*.mp4
*.mov
*.mkv
*.avi
*.wav
*.mp3
*.m4a
*.flac
*.srt

# Shared dev / build tooling (Playwright, PyInstaller)
/.omo
node_modules/
playwright-report/
test-results/

# Local Windows GUI packaging outputs. Keep generated executables local.
/build/
/dist/
/dist/MAW/MAW.exe
*.exe
*.spec.bak
!MAW.spec
.playwright-mcp/
<<<<<<< HEAD
=======

# Uploaded media via Web Console
uploads/

# Console history
.console-history.json

# Runtime results (auto-generated during transcription)
results/

# Local model weights (download manually, several GB each)
models/
!models/.gitkeep

# 实际热词文件（运行时生成，不入库）
/hotwords.txt

# Pip freeze 导出（依赖已在 pyproject.toml 中记录）
requirements.txt
requirements-local.txt

# 开发者工具配置
/.reasonix/
