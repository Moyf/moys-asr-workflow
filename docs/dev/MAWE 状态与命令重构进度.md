# MAWE 状态与命令重构进度

2026-09-26，基线 main `d20529c`，分支 `codex/editor-state-commands`。
维护者要求完成剩余编辑器重构并提交一个整体 PR；Electron 暂缓，由另一条分支负责。
工作区原有未跟踪 `.DS_Store` 保留；不更新根 `blank-editor.html`。

| 项目 | 状态 | 处理与验证 |
| --- | --- | --- |
| starlit-main 现状核对 | 已修复 | 实际远端为 merge/starlit-main，11b7ed2；已有 Electron main / preload、同套件后端管理、Windows 打包与测试。与当前 main 分叉：本线独有 92 提交、该线独有 22；尚未包含 #136 / #154 / #155 的前端目录和模块拆分。只读核对，不修改该分支 |
| 浏览器基线与稳定性 | 已修复 | main 实测 399 项：380 通过 / 19 失败；固定 Control 的测试改用 Playwright ControlOrMeta，文案断言兼容 Cmd；修复 macOS 帮助标签先应用 Cmd 后漏翻译。旧失败所在六份 spec 专项 187 / 187 通过；完整最终回归留到收尾 |
| 状态所有者与选择入口 | 已修复 | MaweState 持有原工程对象、只读选择视图 / 唯一写入入口、偏好、播放 / 面板 / 行内编辑运行态和 dirty 标记；旧门面转发同一状态。5 项状态单测、类型和装配检查通过 |
| 命令 / dirty / 历史事务 | 已修复 | 48 处字幕入口迁移、波形 / 面板暂存事务、保存基线；取消保留 redo，保存与历史边界专项通过 |
| 视图通知与依赖收敛 | 已修复 | 命令明确列表 / 波形 / 预览 / 保存范围，退役旧 pushUndo 和裸叠加轨选择状态；保留尚有消费者的兼容 API |
| 验证 / 文档 / PR | 进行中 | Node 388 全通过；Python 1697 全套成功、8 项既有 skip；浏览器最终回归 / 临时便携产物 / PR 与 CI 继续收尾 |
| Electron / Tauri | 仅说明 | 用户明确暂缓 Electron，另一分支处理；不实现或编译桌面壳 |

## 当前事实与边界

starlit 分支实现通过 localhost Server 复用同一前端，preload 暴露 MOSEDesktop 的工程选择 / 状态能力；窗口启用 contextIsolation / sandbox 并关闭 nodeIntegration。未在本机编译或运行其桌面产物，不以源码审查冒充运行验证。未来集成需要先同步最新 main 的清单与目录，再接其工程打开消息和 Server 契约；本轮保持现有前端 / Server 契约。

本轮按表格顺序推进，一次处理一个当前问题，验收后及时更新本记录。

## 阶段一：基线治理

未改实现时完整 Chromium 399 项，380 通过 / 19 失败。基线还出现副字幕 B 拆分和最短时长拆分两个偶发用例，旧光标 / 列表定位偶发差分本次通过。专项修正后，multi-subtitle、onboarding、overlay-track、waveform-deletion、waveform-history、editor-i18n-save 共 187 项全部通过，无 skip / retry。

平台键修正由已安装 Playwright 的 ControlOrMeta 实现确认，测试没有强制伪装 Windows navigator；保留真实 macOS 路径。英文帮助为实际产品修复，追加 Cmd 标签翻译的单元覆盖；没有关闭失败用例或放宽核心编辑结果断言。

## 阶段二：状态所有权

MaweState 不复制工程对象，选择集合为只读实时视图；48 处选择写入转为 owner 方法，叠加轨的裸选择 / 锚点引用已移除。旧 MaweSelection / MaweCoreState / MaweCuePanelState / MaweInlineEdit 和 dirty 属性仅转发同一状态，保留现有消费者。设置读取填充同一偏好对象，不序列化播放器、DOM 或选择集到工程。

Node 379 项成功；新增状态单测随后扩至 5 项并全部通过；Python 页面 / 清单 30 项、类型、Ruff、语法和装配顺序检查通过。Python 首次指出清单固定契约遗漏新模块，已同步断言。

完整 Chromium 399 项首次 397 通过 / 2 失败：一个测试仍直接使用已移除的裸选择集；另一个换轨入口把只读集传给原地修改的旧 helper，未同步选中结果。前者迁移测试设置入口，后者迁移为 owner.removeAndShift，并补充非顺序选择的重映射覆盖。两项修正后叠加轨专项 24 项全部通过，完整回归在命令迁移后重跑。

## 阶段三：编辑事务与保存基线

48 处字幕历史入口改为 MaweCommands.run / begin：先暂存，实际修改确认后只发布一次。波形拖动预览和面板连续输入只持有暂存事务，取消 / 回到起点保留 redo；无生产消费者的旧 pushUndo 门面已移除。现有文本原生撤销保护和布局 / 空隙 / 预览独立历史仍保留。

核心六份浏览器 spec 181 / 181 通过。新增事务 / 保存用例初次 8 项中 5 通过 / 3 失败，复现保存后撤销错误判断为干净、叠加 / 副轨残留 dirty、聚焦面板保存未提交历史。修正后与保存 / i18n 专项合计 19 / 19 通过。取消测试明确启用已有的 Escape 取消设置，默认仍提交；没有改变用户设置语义。

成功保存记录实际写入的字幕指纹；历史恢复比较这条基线，不回滚其它编辑域的 dirty 状态。保存清理由 owner 覆盖主、副、叠加轨；在途后续编辑不清理。视图适配器明确列表、波形（none / overlay / full）、预览与保存范围，保留新增字幕的滚动策略。

## 阶段四：视图与完整验收

命令统一调度保存后，移除 10 处重复调度，合并 8 处相邻列表 / 预览刷新；选择结果依赖已渲染节点的入口仍保持先列表后选择再预览的顺序。保存和活跃文本输入保留各自必要的局部更新，不把播放帧或每个字符变成全列表广播。

首轮完整 Chromium 409 项为 406 通过 / 3 失败：Escape 用例仍等待暂存输入即时入工程历史，等待期间自动保存改写了 reload 夹具；另有光标 / 瞬态提示差分。调整交互夹具关闭自动保存并保留 reload 前已设置的其它选项，输入断言改为确认前不发布、确认后可撤销；没有降低字幕结果或光标断言。专项首次 30 / 32，暴露初始化脚本覆盖 Escape / 自动滚动设置，已修正为合并设置；最终全套重新运行。

Python 首轮 1697 中一项失败是仍断言旧 waveformLoadedFromProject 声明，更新为 owner 初始化 / 旧门面转发，相关 22 项和完整 1697 项随后成功（8 项既有 skip）。Node 首次环境运行未指定 Python / Chromium，出现 4 个 uv 缓存沙箱失败及 1 个浏览器路径缺失；使用维护环境与已安装 Chromium 后 388 / 388 成功。两类环境失败不记为产品回归。

网站开发页面按源文档单页同步，Astro 15 页构建成功；初次遥测配置目录无权限，通过 ASTRO_TELEMETRY_DISABLED=1 关闭构建遥测后完成。类型、Ruff、清单顺序与语法均通过。历史提取审计 --target d20529c 确认 utils 379 声明、waveform 127 声明 / 177 方法 / 构造器 / 兼容出口；本次拖动事务的行为修改由当前测试覆盖，不混入历史等价声明。

第二轮完整 Chromium 为 408 / 409，唯一失败是双击光标的即时读取；实现本来会在下一轮事件循环重新放置光标。测试改为等待最终 collapsed / text / offset 的完整严格结果，随后重复 12 / 12 成功。最终 Node 再次 388 / 388 成功。完整浏览器最终确认与外部 CI 留待 PR 收尾记录。

真实 Chromium 冒烟：手柄拖动 1500→1680ms、撤销还原、播放 / 暂停、Seek 2000ms、面板输入后 Server 磁盘保存全部成功；当前源码临时生成的 file:// 便携页启动成功，两页无 pageerror，截图自查完成。没有改根 blank-editor.html，也未提交临时媒体或截图。初次冒烟直接改模型而保留旧面板值，保存时被面板提交覆盖；改为真实面板输入后核对落盘文本成功，不把脚本夹具问题记为产品缺陷。

合成三词字幕的纯数据测量（当前 State / Commands、兼容历史快照适配，无 DOM）：1000 条快照 / 完整事务中位数 0.62 / 2.05ms，10000 条为 6.46 / 21.26ms。指纹和历史快照只在开始 / 确认交互时处理，不在每个拖动或播放帧处理；该测量不代表完整浏览器操作耗时。

## 最后光标边界修正

第三轮完整 Chromium 为 407 / 409：真实在途保存用例出现光标 12→6 回退，定位到双击延迟修复仍可能覆盖后续输入；另一个已在基线出现过的副轨 B 用例采用旧坐标点击，工程导入 / 媒体加载重建 lane 后点不到原块。前者观察 beforeinput / keydown / pointerdown 后让新交互拥有光标，并以 session 身份保护同元素新会话；后者改为 Playwright locator 的实时稳定点击，选中、副轨弹窗与主轨数据断言保留。

延迟回调新增 10 项真实入口 / 受控 DOM 调度契约：修正前 2 通过 / 8 失败，修正后 10 / 10 通过；完整 Node 398 / 398。曾尝试的浏览器时钟夹具混入原生焦点恢复，无法隔离目标，已撤掉该夹具，原生输入由浏览器在途保存用例继续覆盖。真实输入、双击与副轨点击重复验证及最终全套继续执行。
