# MAWE 职责拆分与宿主边界进度

2026-09-26，继续在 PR #155 / `codex/editor-wiring-layout` 实施。
本阶段基线 `28f33e2`；工作区仅有原有未跟踪 `.DS_Store`。

维护者明确：继续同一 PR，补拆 `waveform.js`；Tauri 完整编译不再验收，后续桌面方向为 Electron。

| 项目 | 状态 | 处理与验收 |
| --- | --- | --- |
| utils 职责拆分 | 已修复 | 原文件 6,988 行；按实际依赖图拆纯数据领域，保留 AsrEditorUtils 出口和函数实现；重复声明须保持原来最后声明生效语义 |
| waveform 职责拆分 | 已修复 | 原文件 6,489 行，WaveformEditor 类约 4,829 行；分离布局、解码、时序算法、绘制、指针和拖动；类方法用描述符复制保留 getter 和非枚举属性 |
| 文件 / 保存 / 运行环境接口 | 已修复 | 为浏览器和未来 Electron 留显式可注入接口，先接管设置存储、下载、工程保存与 Server 请求；不实现 Electron 壳 |
| 自动化与装配验收 | 已修复 | 保持现有 Python / Server / 单文件产物契约；源码级迁移审计、Node / Python / typecheck / Ruff；本阶段新增包装与依赖注入，不再要求整体拼接 AST 与机械拆分前相同 |
| 浏览器与便携验收 | 已修复 | 同环境基线差分；实际工程选择、播放、seek、拖动、保存、撤销及布局验证；临时便携产物，不更新根 blank-editor.html |
| PR 与文档收尾 | 已修复 | 整体一个 PR，内部按检查点提交；同步源码地图、验证边界与最终 PR 描述 |
| 合并前宿主方法接收者复查 | 已修复 | utils 使用回调转发 getNavigator，保留自定义 runtime 的 this；回归用例先复现启动 TypeError，修复后通过，并验证 navigator 更新仍可见 |
| 全量 Store / 命令迁移 | 仅说明 | 本阶段仅整理依赖与宿主边界；统一状态、命令和历史归属另阶段推进 |

## 实施记录

- 事实基线：保留经典脚本单文件装配。现有 `window.MAWE` 已提供模块工厂注册与解析，可用来显式注入跨领域依赖，不增加生产依赖。
- 发现两个已有重复声明：`cloneJsonValue`、`normalizeKeyboardOperationReferenceMode`。旧 IIFE 使用后者覆盖前者；迁移不能让不同领域各自绑定到不同实现。

阶段记录：utils 拆为 30 个领域工厂，兼容门面 500 行；waveform 拆为 9 个辅助领域与 14 组类方法，兼容装配与构造器 271 行。实际行数以最终源码地图为准。

验证：`node scripts/check_editor_domains.mjs` 比较基线 `28f33e2`，379 条 utils 语句、127 条波形辅助语句、177 个类方法源码完全相同；构造器和兼容导出原样保留。相关 Node 336 项、新增跨模块可变状态 / getter 描述符测试 3 项通过；Python 装配 / 波形 44 项通过；typecheck、Ruff、diff 检查通过。浏览器与全量验证留在后续验收行，未以单测替代。

阶段记录：宿主工厂在业务加载前初始化，存储、选择器 / 文件写入 / 下载、Server URL 与 fetch、navigator / 用户手势均可替换。工程保存、另存为、新建检查点、SRT / 时间线 / 动态导出、最近工程、连接检查与工作区请求已接入；波形设置通过参数注入 storage，utils 的 gap core 与平台默认值也显式注入。未实现 Electron 主进程 / IPC / 打包，未重构 Canvas 热路径。

验证：Node 最终全量 373 项通过（包含新增宿主测试 6 项：无能力启动、存储拒绝、写入顺序与失败、picker 取消、下载清理、请求信号与 URL、完整宿主替换）；Python 1,697 项成功（8 skipped）；相关类型检查、Ruff 与 diff 检查通过。首次 Python 路径更新遗漏了单条断言的 edit 导入，已修正并重跑全量；首次 Node VM 断言跨 realm TypeError 识别已修正。完整 Playwright 已记录为差分结果，不宣称全量通过。

浏览器验收：全量 399 项，本树 379 通过 / 20 失败；同环境机械拆分前基线 381 通过 / 18 失败，基线 18 项在本树全部仍失败。额外两项分别为 B 拆分的列表定位（91 vs 92）和保存请求在途时光标偏移（6 vs 12）；在本树和基线各串行 repeat 3（每边共 6 次）全部通过，未观察到稳定独有回归。保留完整失败账本，未把偶发差异写成全量全绿。

实际 Chromium 工程冒烟：右边界从 1500ms 拖至 1680ms，撤销回 1500ms；播放 / 暂停、seek 到 2000ms、Server 保存到真实临时工程文件通过。运行时类原型包含 178 个自有属性（构造器 + 177 方法 / getter）；durationMs getter 为非枚举。工程页与新生成临时便携页 pageerror 均为 0，两张截图已检查。临时脚本最初误用了历史方法名和播放按钮 id，修正后完成，未作为产品故障归因。

质量复查：文件写入使用 Blob 构造回调，在 createWritable 完成后才构造正文，保留旧实现对新建 / 另存为的取值时机；宿主测试断言 open → build → write → close 顺序。此最后调整后，实际浏览器冒烟已复跑通过，另对文件 / 保存相关场景补跑专项回归。

产物层：Astro 15 页构建成功；临时便携 HTML 生成成功，root blank-editor.html 未更新。源码地图与官网开发文档已同步，仅生成 development 一页，没有重写其他公共文档。

## 收尾

写入时序最后调整后，Node 全量再次 373 / 373 通过，Python 再次 1,697 项成功（8 skipped）；新建 / 打开 / 另存为 / 保存 / 断线恢复 / FCP7 导出专项 34 / 34 通过。两项偶发差分各在两边串行重复 3 次通过；实际浏览器冒烟复跑通过。typecheck、Ruff、diff 检查通过，本阶段 90 份新增 / 修改文本检查 UTF-8 / LF 通过。

本阶段实现与本地验收完成，仍在统一 PR #155 中。utils 门面 500 行，waveform 门面 / 构造器 271 行；新增领域文件最大为 cue-blocks.js 781 行，不把行数缩减当作状态所有权已经解决。跨接线裸引用与全量 Store / 命令仍是后续范围，Electron 壳未实施。

没有新增用户行为，不新增 CHANGELOG 条目。内联副本待发布前统一重生成；根 blank-editor.html 不变。原有 .DS_Store 未提交。完整 Tauri 编译免除，本阶段没有该阻塞项。远端最终提交和 CI 以 PR 状态为准。

合并前复查：自定义 runtime 的 getNavigator 方法依赖 this 时，utils 直接传函数引用导致 ASS 默认字体初始化失败。新增回归用例先确认该 TypeError，再改为保留接收者的回调转发。最终 Node 374 / 374 通过；源码审计、typecheck、Ruff、diff 检查通过；实际工程拖动、撤销、播放 / 暂停、seek 与 Server 保存冒烟复跑通过。最初 Node 复跑漏指定已安装 Chromium 路径，属于测试环境配置错误，补齐路径后全量通过。Python 与完整浏览器差分仍采用上述本阶段证据，未把本次冒烟当作完整浏览器全绿。
